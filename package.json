{
  "name": "@geolonia/mbgl-gesture-handling",
  "version": "1.0.9",
  "description": "This is a Mapbox GL JS plugin that handles wheel and touch gestures to prevent unexpected zooming and moving for the map embeded in your web site.",
  "main": "mbgl-gesture-handling.js",
  "scripts": {
    "start": "parcel -d ${TMPDIR} example/index.html --open",
    "build": "npm run build:site",
    "build:site": "rm -fr docs && parcel build -d docs example/index.html --public-url '.' && touch docs/.nojekyll",
    "test": "eslint \"mbgl-gesture-handling.js\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geolonia/mbgl-gesture-handling.git"
  },
  "author": "geolonia.com",
  "license": "MIT",
  "devDependencies": {
    "@geolonia/mbgl-fork-me-control": "^0.2.7",
    "@geolonia/mbgl-geolonia-control": "^0.3.8",
    "babel-polyfill": "^6.26.0",
    "eslint": "^6.8.0",
    "http-server": "^0.12.1",
    "parcel-bundler": "^1.12.4"
  }
}
